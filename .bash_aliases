# tail that follow the file by file name after rolled
alias tailer="tail --follow=name --retry"

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias lt='ls -altF'

# git show lastest changes
alias gitl='git show HEAD'
